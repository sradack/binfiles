#!/usr/bin/env bash
# Set up a tmux workspace: rename window, split a pane, and launch claude

if [ -z "$TMUX" ]; then
  echo "Not in a tmux session" >&2
  exit 1
fi

tmux rename-window "$(basename "$PWD")"
tmux split-window -h -c "$PWD"
claude
